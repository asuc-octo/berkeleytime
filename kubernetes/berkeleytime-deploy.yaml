apiVersion: apps/v1
kind: Deployment
metadata:
  name: berkeleytime-prod
  namespace: berkeleytime
  labels:
    appName: berkeleytime-prod
spec:
  selector:
    matchLabels:
      appName: berkeleytime-prod
  replicas: 4
  template:
    metadata:
      labels:
        appName: berkeleytime-prod
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
    spec:
      containers:
      - name: berkeleytime-prod
        imagePullPolicy: Always
        image: index.docker.io/berkeleytime/berkeleytime
        command: ["python", "manage.py", "runserver"]
        env:
        - name: PYTHONUNBUFFERED
          value: "0"
        - name: GET_HOSTS_FROM
          value: dns
        envFrom:
        - configMapRef:
            name: django-configs
        - secretRef:
            name: aws
        - secretRef:
            name: google
        - secretRef:
            name: sis
        - secretRef: 
            name: sendgrid
        - secretRef:
            name: sentrydsn
        ports:
        - containerPort: 8000
