// home.js
// javascript for the homepage of berkeleytime

// Colors of fields:
// All fields have black text when focused: this is okay because placeholder text
// always goes away on focus. However, when we have a text or password field that
// is blurred with data in it, we have to set the css property 'color' to 'black';
// then any time we replace the placeholder text, we have to set it back to '#999999'.

$(document).ready(function () {

    // $('#create_non_fb, #login_existing').click(function() {
    //  $('#register_container').toggle(300, 'linear', function (){
    //      $('#login_container').toggle(300, 'linear');
    //  });
    //  // $('#form_container').addClass('active');
    // });

});
