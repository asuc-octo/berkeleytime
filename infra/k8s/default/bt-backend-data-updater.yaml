---
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: bt-backend-data-updater-dev-node
  namespace: default
spec:
  concurrencyPolicy: Replace
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      activeDeadlineSeconds: 64800
      template:
        metadata:
          labels:
            component: bt-backend-data-updater-dev-node
        spec:
          shareProcessNamespace: true
          containers:
            - command:
                - node
                - build
              env:
                - name: NODE_ENV
                  value: privileged
              envFrom:
                - secretRef:
                    name: bt-backend-dev-node
              image: berkeleytime.com:5000/berkeleytime/berkeleytime/bt-backend:node
              imagePullPolicy: Always
              name: bt-backend-data-updater-dev-node
            - command:
                - bash
                - -c
                - |
                  curl http://localhost:5000/api/sis_courses/requestDump
                  curl http://localhost:5000/api/sis_courses/parseDump
                  curl http://localhost:5000/api/sis_classes/requestClassDump
                  curl http://localhost:5000/api/sis_class_sections/requestClassSectionDump
                  curl http://localhost:5000/api/calanswers_grades/parseGradeDump
                  pkill node
              env:
                - name: NODE_ENV
                  value: privileged
              envFrom:
                - secretRef:
                    name: bt-backend-dev-node
              securityContext:
                runAsUser: 0
              image: curlimages/curl
              imagePullPolicy: Always
              name: curl
              ports:
                - containerPort: 5000
                  protocol: TCP
              startupProbe:
                failureThreshold: 30
                httpGet:
                  path: /health
                  port: 5000
                periodSeconds: 10
          restartPolicy: OnFailure
  schedule: 0 0 * * *
  successfulJobsHistoryLimit: 1
