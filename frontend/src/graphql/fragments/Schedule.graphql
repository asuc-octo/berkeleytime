fragment Schedule on ScheduleType {
  id
  year
  semester
  name
  totalUnits
  dateCreated
  dateModified

  selectedSections {
    edges {
      node {
        ...SectionSelection
      }
    }
  }
}
